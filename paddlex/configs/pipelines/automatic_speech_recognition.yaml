pipeline_name: chunk_conformer

SubModules:
  SpeechRecognition:
    module_name: chunk_conformer
    model_name: Conformer
    model_dir: null
    batch_size: 1
    sample_rate: 16000
    chunk_length: 30
    stride: 10
