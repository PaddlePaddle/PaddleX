hpi_config:
  backend_config:
    openvino:
      cpu_num_threads: 8
      enable_mkldnn: false
    paddle_infer:
      cpu_num_threads: 8
      enable_log_info: false
      enable_mkldnn: false
  selected_backends:
    cpu: openvino
    gpu: paddle_infer
  supported_backends:
    cpu:
    - paddle_infer
    - openvino
    gpu:
    - paddle_infer
